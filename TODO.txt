Tests für Verhalten des Dialogs schreiben. Kommentare für die verteilte logik und neue "Architektur" verwertung der Eingaben je nach Page, Displays je nach page im driver festgelegt. 

Möglicherweise naming des lcd_states ändern, wenn es jetzt eher ein zentraler Zustand ist, von dem controller steuerung auch ahängt. 

Data Buffer überdenken.Tests für Verhalten des Dialogs schreiben. Kommentare für die verteilte logik und neue "Architektur" verwertung der Eingaben je nach Page, Displays je nach page im driver festgelegt. 

Möglicherweise naming des lcd_states ändern, wenn es jetzt eher ein zentraler Zustand ist, von dem controller steuerung auch ahängt. 

Data Buffer überdenken.  

